---
common_packages_redhat:
  - chrony
  - bind-utils
  - tar

common_packages_debian:
  - systemd-timesyncd
  - dnsutils

common_ntp_service: >-
  {{ 'chronyd' if ansible_facts['os_family'] == 'RedHat' else 'systemd-timesyncd' }}
